# useHash

A React hook for getting hash value in url.

```js

import useHash from 'use-hash'

const Component = () => {
}

export default () => {
  const hash = useHash()
  return <div>{hash}</div>
}


```
